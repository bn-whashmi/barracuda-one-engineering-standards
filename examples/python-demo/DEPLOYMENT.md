# Deployment

The demo is intended for local execution and CI validation. There is no
production deployment target.

Before introducing one, document its environment, build command, rollback
path, required secrets, and artifact verification requirements here.
