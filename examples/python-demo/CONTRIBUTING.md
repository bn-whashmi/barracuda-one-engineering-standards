# Contributing

1. Make a focused change and add or update tests.
2. Run the local test and validation commands from `README.md`.
3. Open a pull request; do not push directly to the default branch.
4. Resolve findings and review the generated guardrail scorecard before merge.

The repository's `AGENTS.md`, architecture, standards, testing, and security
documents are the application ground truth for this demo.
