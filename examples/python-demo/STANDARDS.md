# Engineering Standards

- Keep application logic in `app.py` and tests in `test_app.py`.
- Prefer the Python standard library unless a dependency is justified.
- Keep functions small, deterministic, and explicit about invalid input.
- Do not commit credentials, generated reports, or local machine paths.
- Every behavior change requires a focused unit test and a pull request.
