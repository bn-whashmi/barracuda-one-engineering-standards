# Sample Application Ground Truth

- Keep application logic in `app.py` and tests in `test_app.py`.
- Use the Python standard library unless a dependency is justified.
- Every behavior change requires a focused unit test.
- Run `python3 -m unittest discover -s . -p 'test_*.py'` before opening a PR.
