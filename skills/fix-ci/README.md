# Fix CI

Phase 2 placeholder.

Intended scope: diagnose a failing CI check, reproduce it where possible,
apply the smallest safe fix, and verify the relevant workflow and tests.

Related canonical skills: `skills/github-actions-hardening` and
`skills/full-test-suite`.
